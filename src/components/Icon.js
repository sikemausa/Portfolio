import React, { Component } from 'react';

class Icon extends Component {
  render() {
    return (
      <section id="Icon">
        <img src="" />
        <h3>{this.props.text}</h3>
      </section>
    );
  }
}

export default Icon;
